{
  "name": "cartas",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --filter @cartas/web dev",
    "build": "pnpm -r --filter @cartas/web build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
